import { FrameScrubber } from '../ui/FrameScrubber';

export function CinematicVideo() {
  return (
    <section id="cinematic" className="relative bg-black w-full overflow-hidden">
      <FrameScrubber />
    </section>
  );
}
